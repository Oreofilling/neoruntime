# API 参考

完整的 REST/WebSocket/SSE 接口面——23 个功能分组共 232 个操作，渲染自仓库的 OpenAPI 3.0 规范（`docs/api/swagger.yaml`）。

使用**面板内的搜索框**可按路径、摘要或参数名查找操作；面板自带下载按钮可导出原始规范。顶栏的深浅色切换会同步改变参考页配色。

<ApiReference spec="swagger.zh.json" />
