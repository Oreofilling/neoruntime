# API Reference

The complete REST/WebSocket/SSE surface — 232 operations across 23 functional groups, rendered from the repository's OpenAPI 3.0 spec (`docs/api/swagger.yaml`).

Use the **search box inside the panel** to find operations by path, summary or parameter name; the panel's own download button exports the raw spec. The dark/light toggle in the top bar also restyles the reference.

<ApiReference spec="swagger.json" />
